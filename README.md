# Machine-Learning-Algorithms

This repository contains implementations of the most common and popular machine learning algorithms, written entirely from scratch in C++ — without using any external frameworks or libraries.

The goal is educational: to understand how each algorithm works at the lowest level by building everything ground-up, including the underlying data structures (vectors, matrices, etc.) and all numerical operations.